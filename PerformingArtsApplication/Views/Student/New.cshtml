
@{
    ViewBag.Title = "New";
}

<script>
    //client side validation
    function validateStudentForm() {
        var validStudentData = true;

        var StudentId = document.getElementById("StudentId").value;
        var StudentIdError = document.getElementById("StudentIdError");

        if (StudentId === "") {
            StudentIdError.style.display = "block";
            validStudentData = false;
        } else {
            StudenIdError.style.display = "none";
        }

        var FirstName = document.getElementById("FirstName").value;
        var FirstNameError = document.getElementById("FirstNameError");

        if (FirstName === "") {
            FirstNameError.style.display = "block";
            validDancerData = false;
        } else {
            FirstNameError.style.display = "none";
        }

        var LastName = document.getElementById("LastName").value;
        var LastNameError = document.getElementById("LastNameError");

        if (LastName === "") {
            LastNameError.style.display = "block";
            validDancerData = false;
        } else {
            LastNameError.style.display = "none";
        }

        var Birthday = document.getElementById("Birthday").value;
        var BirthdayError = document.getElementById("BirthdayError");

        if (Birthday === "") {
            BirthdayError.style.display = "block";
            validStudentData = false;
        } else {
            BirthdayError.style.display = "none";
        }

        if (validStudentData === false) {
            return false;
        } else {
            return true;
        }
    }
</script>

<h2>Create Student</h2>

<form action="/Student/Create" method="POST" onsubmit="return validateStudentForm()">
    <div class="formInput">
        <p id="StudentIdError" style="display:none; color:red;">*Please enter a student id</p>
        <label>Student Id</label>
        <input type="number" name="StudentId" id="StudentId" />
    </div>
    <div class="formInput">
        <p id="FirstNameError" style="display:none; color:red;">*Please enter a first name</p>
        <label>First Name</label>
        <input type="text" name="FirstName" id="FirstName" />
    </div>
    <div class="formInput">
        <p id="LastNameError" style="display:none; color:red;">*Please enter a last name</p>
        <label>Last Name</label>
        <input type="text" name="LastName" id="LastName" />
    </div>
    <div class="formInput">
        <p id="BirthdayError" style="display:none; color:red;">*Please enter a birthday</p>
        <label>Birthday</label>
        <input type="date" name="Birthday" id="Birthday" />
    </div>
    <div>
        <input class="submitBtn" id="addStudentSubmit" type="submit" value="Add Student" />
    </div>
</form>